# Neovim ショートカット一覧

このドキュメントは、現在の Neovim 設定で使えるキーバインドをまとめたものです。

## 基本情報

| 項目 | 値 |
|---|---|
| リーダーキー (`<leader>`) | `,`（カンマ） |
| ローカルリーダー | `,`（カンマ） |
| ヘルプ言語 | 日本語優先（`helplang=ja,en`） |

`which-key.nvim` により、`,` などを押すと続けられるキーの候補が表示されます。

---

## 基本操作

| キー | 操作 |
|---|---|
| `<Esc>` | 検索ハイライトを消去（ノーマルモード） |
| `<Esc><Esc>` | ターミナルモードを終了 |
| `q` | マクロ録画の終了（`recording @a` 表示中） |

### マクロ

| キー | 操作 |
|---|---|
| `q` + レジスタ名 | マクロ録画開始（例: `qa`） |
| `q` | マクロ録画終了 |
| `@` + レジスタ名 | マクロ再生（例: `@a`） |
| `@@` | 直前のマクロを繰り返し |

---

## ウィンドウ（分割）

| キー | 操作 |
|---|---|
| `Ctrl + h` | 左のウィンドウへ移動 |
| `Ctrl + l` | 右のウィンドウへ移動 |
| `Ctrl + j` | 下のウィンドウへ移動 |
| `Ctrl + k` | 上のウィンドウへ移動 |
| `,ws` | 水平分割（上下） |
| `,wv` | 垂直分割（左右） |
| `,wc` | 現在のウィンドウを閉じる |
| `,wq` | 現在のウィンドウを閉じる（別名） |
| `,wo` | 他のウィンドウをすべて閉じる（最大化） |

`Ctrl + h` が効かない場合は `<C-w> h`（`<C-w>` を押してから `h`）でも移動できます。

---

## タブ

| キー | 操作 |
|---|---|
| `,tn` | 新しいタブを開く |
| `,tc` | 現在のタブを閉じる |
| `,to` | 他のタブをすべて閉じる |
| `,t]` | 次のタブ |
| `,t[` | 前のタブ |
| `gt` | 次のタブ（標準） |
| `gT` | 前のタブ（標準） |

---

## ターミナル・CLI

| キー | 操作 |
|---|---|
| `,tt` | 右側にターミナルを開く |
| `,tT` | 下側にターミナルを開く |
| `,mi` | 右側に Gemini CLI を開く |
| `,cu` | 右側に Cursor Agent を開く |

ターミナルからエディタに戻る: `<Esc><Esc>`

---

## 検索（Telescope）

| キー | 操作 |
|---|---|
| `,sf` | ファイル検索 |
| `,sg` | ライブ grep（プロジェクト全体） |
| `,sw` | カーソル下の単語を検索 |
| `,sh` | ヘルプ検索 |
| `,sk` | キーマップ検索 |
| `,sd` | 診断検索 |
| `,sr` | 前回の検索を再開 |
| `,s.` | 最近開いたファイル |
| `,ss` | Telescope ピッカー一覧 |
| `,sn` | Neovim 設定ファイルを検索 |
| `,,` | 開いているバッファ一覧 |
| `,/` | 現在のバッファ内をファジー検索 |
| `,s/` | 開いているファイル内を grep |

### Telescope 内の操作

| キー | 操作 |
|---|---|
| `<C-/>` | キーマップ一覧（インサートモード） |
| `?` | キーマップ一覧（ノーマルモード） |

---

## ファイルエクスプローラー（neo-tree）

| キー | 操作 |
|---|---|
| `\` | ファイルツリーの表示/非表示 |
| `,e` | ファイルツリーの表示/非表示 |
| `,E` | ファイルツリーにフォーカス |

### ファイルツリー内

| キー | 操作 |
|---|---|
| `\` | ウィンドウを閉じる |
| `q` | ウィンドウを閉じる |

---

## Git

### vim-fugitive

| キー | 操作 |
|---|---|
| `,gc` | コミット |
| `,gC` | コミット amend |
| `,gs` | Git ステータス |
| `,gd` | 差分表示（分割） |
| `,gp` | push |
| `,gP` | pull |

### gitgraph.nvim

| キー | 操作 |
|---|---|
| `,gl` | コミットグラフを表示 |

グラフ内:

| キー | 操作 |
|---|---|
| `Enter` | 選択コミットの差分を Diffview で表示 |
| ビジュアルモードで範囲選択 | 選択範囲の差分を表示 |

### gitsigns.nvim

カスタムキーマップは未設定。ガター（行番号左）に変更マークを自動表示します。

| 記号 | 意味 |
|---|---|
| `+` | 追加 |
| `~` | 変更 |
| `_` | 削除 |

---

## LSP（言語サーバー）

LSP が接続されたバッファでのみ有効です。

| キー | 操作 |
|---|---|
| `grd` | 定義へ移動 |
| `grD` | 宣言へ移動 |
| `grr` | 参照を検索 |
| `gri` | 実装へ移動 |
| `grt` | 型定義へ移動 |
| `grn` | 名前変更 |
| `gra` | コードアクション |
| `gO` | ドキュメント内シンボル検索 |
| `gW` | ワークスペース内シンボル検索 |
| `,th` | インレイヒントの ON/OFF |
| `,q` | 診断一覧（quickfix）を開く |
| `<C-t>` | ジャンプ前の位置に戻る |

---

## フォーマット（conform.nvim）

| キー | 操作 |
|---|---|
| `,f` | 現在のバッファを手動フォーマット |
| 保存時 | 自動フォーマット（C/C++ を除く） |

Lua ファイルは `stylua` で整形されます。

---

## 自動補完（blink.cmp）

インサートモードで使用します。

| キー | 操作 |
|---|---|
| `Ctrl + Space` | 補完メニューを開く / ドキュメント表示 |
| `Ctrl + n` / `Ctrl + p` | 次 / 前の候補 |
| `Ctrl + y` | 補完を確定 |
| `Ctrl + e` | メニューを閉じる |
| `Ctrl + k` | シグネチャヘルプの切り替え |
| `Tab` / `Shift + Tab` | スニペット内の移動 |

---

## mini.nvim

### mini.ai（テキストオブジェクト）

| キー | 操作 |
|---|---|
| `va)` | 括弧の周囲を視覚選択 |
| `ci'` | 引用符の内側を変更 |
| `yinq` | 次の引用符の内側をヤンク |

### mini.surround（囲み文字）

| キー | 操作 |
|---|---|
| `saiw)` | 単語を `()` で囲む |
| `sd'` | 引用符の囲みを削除 |
| `sr)'` | `)` を `'` に置換 |

---

## 管理コマンド

| コマンド | 操作 |
|---|---|
| `:Lazy` | プラグイン管理 |
| `:Lazy update` | プラグイン更新 |
| `:Mason` | LSP サーバー・ツール管理 |
| `:help` / `:h` | ヘルプ（日本語優先） |
| `:Tutor ja` | 日本語チュートリアル |
| `:checkhealth` | 設定の健全性チェック |

---

## よく使うショートカット（クイックリファレンス）

```
,sf   ファイル検索
,sg   プロジェクト全体を grep
,e    ファイルツリー
,f    フォーマット
,gs   Git ステータス
,gl   コミットグラフ
,mi   Gemini CLI
,cu   Cursor Agent
grd   定義へ移動
Ctrl+Space  補完
```

---

最終更新: 2026年7月
