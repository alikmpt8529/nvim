{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in */doc/kickstart.txt) \"$(dirname \"$f\")/txt2md.sh\" \"$f\" > \"$(dirname \"$f\")/kickstart.md\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
